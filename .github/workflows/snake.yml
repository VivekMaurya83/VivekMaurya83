name: Generate GitHub Profile Assets

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:

      # 📥 Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🐍 Contribution Snake
      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      # 🏆 GitHub Trophies (developer-focused only)
      - name: Generate GitHub Trophies
        uses: soulteary/github-profile-trophy-action@v1.0.0
        with:
          options: >-
            username=${{ github.repository_owner }}&
            theme=radical&
            title=Commits,Repositories,Stars,Followers,PullRequest,Issues,Reviews&
            column=7&
            row=1&
            margin-w=12&
            no-bg=true&
            no-frame=true
          path: dist/github-trophies.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      # 🚀 Publish generated assets
      - name: Push Profile Assets to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
